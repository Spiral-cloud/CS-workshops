using System.Diagnostics.Tracing;
using System.Runtime.CompilerServices;
using System.Linq;

internal class Program
{
    private static void Main(string[] args)
    {
        // double[] temperatures = new double[7];
        // for (int i = 0; i <= temperatures.Length - 1; i++)
        // {
        //     Console.Write("Enter a temperature: ");
        //     temperatures[i] = Convert.ToDouble(Console.ReadLine());
        // }
        // Array.Sort(temperatures);
        // Console.WriteLine(string.Join(", ", temperatures));

        // int[] student_scores = new int[5];
        // for (int i = 0; i <= student_scores.Length - 1; i++)
        // {
        //     Console.Write("Enter a grade: ");
        //     student_scores[i] = Convert.ToInt32(Console.ReadLine());
        // }
        // int average = (student_scores[0] + student_scores[1] + student_scores[2] + student_scores[3] + student_scores[4]) / student_scores.Length;
        // Console.WriteLine($"The average of all the scores is: {average}");
        // Array.Reverse(student_scores);
        // Console.WriteLine(string.Join(", ", student_scores));
        // Console.Write("Enter the score you want to search for: ");
        // int search_int = Convert.ToInt32(Console.ReadLine());
        // int index = Array.IndexOf(student_scores, search_int);
        // Console.WriteLine($"{search_int} appears at index: {index + 1}");

        // List<string> shoppingList = new List<string>();
        // bool repeat = true;
        // while (repeat == true)
        // {
        //     Console.Write("Add a value to the shopping list (Type end if completed): ");
        //     string item = Console.ReadLine()!;
        //     if (item == "end" || item == "End")
        //     {
        //         repeat = false;
        //     }
        //     else
        //     {
        //         shoppingList.Add(item);
        //     }
        // }
        // foreach (string i in shoppingList)
        // {
        //     Console.WriteLine(i);
        // }
        // Console.Write("Which item do you want to remove: ");
        // string remove_choice = Console.ReadLine()!;
        // shoppingList.Remove(remove_choice);
        // foreach (string i in shoppingList)
        // {
        //     Console.WriteLine(i);
        // }

        // List<string> items = new List<string>();
        // List<double> prices = new List<double>();
        // bool repeat = true;
        // int counter = 0;
        // while (repeat == true)
        // {
        //     Console.Write("Enter an item to add to the list: ");
        //     string item = Console.ReadLine()!;
        //     items.Insert(counter, item);
        //     Console.Write("Enter the price of the item: ");
        //     double price = Convert.ToDouble(Console.ReadLine());
        //     prices.Insert(counter, price);
        //     Console.Write("Do you want to add another item? (yes/no)");
        //     string item_choice = Console.ReadLine()!;
        //     if (item_choice == "no")
        //     {
        //         repeat = false;
        //     }
        //     else
        //     {

        //     }
        //     counter++;
        // }
        // double sum = 0;
        //     for (int i = 0; i <= prices.Count() - 1; i++)
        //     {
        //         Console.WriteLine($"{items[i]} price: {prices[i]}");
        //         sum = prices[i] + sum;

        //     }
        // Console.WriteLine(sum);
       


    }
}